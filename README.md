# Planet Hunter Plotter

This is a simple project using Plotly to plot a given light curve from planet hunters using python

To use:
```
pip install -r requirments.txt
python3 plot.py path/to/.dat/file
```